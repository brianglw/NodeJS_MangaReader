<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manga Scraper</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <link rel="stylesheet" href="styles/main.css">
</head>
<body>
    <%- include('partials/header.ejs') %>
    <!-- User Step 1: Type in manga title -> /query -> renders to locals.data below -->

    <section class="jumbotron text-center">
        <div class="container">
        <form action="/query" method="post">
            <h1 class="jumbotron-heading">Search by Title: </h1>
            <input class="btn btn-light my-2" type="text" name="title" placeholder="Sousou no Frieren"/>
            <input class="btn btn-primary my-2" type="submit" />
        </form>
        </div>
    </section>
    
    <!-- User Step 2: User clicks on form -> /chapterlist -> renders a list of chapters to chapterlist.ejs -->
    <% if (locals.data) { %>
        <div class="album py-5 bg-light">
            <div class="container">
            <div class="row">
                <% data.forEach(manga => { %>
                <div class="col-md-3">
                <div class="card mb-4 box-shadow">
                    <img class="card-img-top" data-src="holder.js/100px225?theme=thumb&amp;bg=55595c&amp;fg=eceeef&amp;" alt="<%=manga?.title_jp%>" style="display: inline;" src="<%= manga?.cover %>" data-holder-rendered="true">
                    <div class="card-body">
                        <p class="card-title"><%=manga?.title_en%> (EN)/ <%=manga?.title_jp%> (JP)</p>
                        <p class="card-text"><%=manga?.desc%></p>
                        <div class="d-flex justify-content-between align-items-center">
                            <div class="btn-group">
                                <form method="post" action="/chapterlist">
                                    <input type="hidden" name="mangaID" value="<%=manga.id%>"/>
                                    <input type="submit" value="Read" />
                                </form>
                            </div>
                            <small class="text-muted"><%=manga?.status%></small>
                        </div>
                    </div>
                </div>
                </div>
            <% }); %>
            </div>
            </div>
        </div>
    <% } %>
    

    <%- include('partials/footer.ejs') %>
</body>
</html>

